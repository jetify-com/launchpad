syntax = "proto3";
package api;
option go_package = "go.jetpack.io/launchpad/proto/api";

enum Environment {
  // NONE ensures Environment is always explicitly sent by the client
  NONE = 0;
  DEV = 1;
  PROD = 2;
  STAGING = 3;
}
